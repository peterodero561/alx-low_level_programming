/*
 * File: 5-rev_string.c
 * Auth: Peter
 */

#include "main.h"

/**
 * rev_string -reverses a string
 * @s: string to be reversed
 *
 * Return: Always 0
 */

void rev_string(char *s)
{
	int x;
	int y;

	for (x = 0; s[x] != '\0'; x++)
	{
	}
	for (y = x - 1; y >= 0; y++)
	{
		temp = s[y];
	}
}
